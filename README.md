runelite-cache-code-updater
===========================

This takes things in the runelite repo and automagically maps them to the
new values when the cache changes. Most important is script and widget updates.